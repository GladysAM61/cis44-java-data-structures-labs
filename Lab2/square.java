package com.mycompany.lab2;

/**
 *
 * @author gladysarias
 */
public class square extends quadrilateral{
    //a square has all the same sides so we just add one side to both length and width
    public square(double side){
        super(side,side);
    }
}
